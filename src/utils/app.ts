import { AppMenu } from '@/models/app';

const APP_NAME: string = 'AdaptiveNetworks';

const APP_MENU: AppMenu[] = [
    {
        title: undefined,
        name: 'Adaptive Networks',
        ccid: undefined,
        icon: 'icon-logo-centurylink',
    },
];

export { APP_NAME, APP_MENU };
